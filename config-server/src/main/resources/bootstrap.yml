server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/miguel-armas-abt/sistema-compras-backend.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: s3cr3t
